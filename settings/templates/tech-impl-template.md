# [需求名称]代码实现方案

## 1. 概述

**模块清单**:

[按行列出本次需生成代码实现方案涉及的所有业务模块，每个模块一行，一句话说明]

---

## 2. 详细实现方案
<!-- 编写要求：
   1. 读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `系统模式` ，确定应用架构和分层并以此编写全部的子章节
   2. 完全复用的类、接口、代码、文件列出关键信息即可，无需列出完整信息
-->

### 2.1 常量和枚举

[用表格列出所有常量和枚举，为新增和修改的类提供关键代码。如无常量和枚举，省略本章节]

| 类名 | 文件路径 | 说明 | 所有常量值或枚举值 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- |
| [类名] | [完整路径] | [用途说明] | [所有常量值或枚举值] | [技术方案X.X/规范文档] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如命名规范、使用场景等，不能只写参考XXX]

**关键代码**:

```java
// 每种类型列出5-10行关键代码，体现类声明、注解、核心常量定义
```

---

### 2.2 数据模型
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `持久层规范` 完整内容并严格遵守-->

[用表格列出所有数据模型，为新增和修改的类提供关键代码。如无数据模型，省略本章节]

| 类名 | 文件路径 | 说明 | 所有字段和类型 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- |
| [类名] | [完整路径] | [用途说明] | [所有字段和类型] | [技术方案X.X/规范文档] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如命名规范、使用场景等，不能只写参考XXX]

**关键代码**:

```java
// 每种类型列出5-10行关键代码，体现类声明、注解、核心常量定义
```

---

### 2.3 持久层[依据应用架构调整名称]
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `持久层规范` 完整内容并严格遵守-->

[用表格列出所有持久层的类、接口和文件（Mapper/MapperExt/DAO/Criteria/Mapper.xml等），为新增和修改的类和文件提供关键代码。如无持久层，省略本章节]

| 名称 | 文件路径 | 对应数据模型 | 所有方法 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- |
| [类/接口/文件名称] | [完整路径] | [数据模型名] | [所有方法，标注变更类型] | [技术方案X.X/规范文档] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如继承的父接口/类、必须字段、注解、配对文件（XML/MapperExt）、自定义SQL等，不能只写参考XXX]

#### 2.3.1 [名称]实现方案（[新增/修改]）

**实现要点**:
- **相关注解**: [列出注解]
- **相关依赖**: [列出依赖]

**方法清单**:

| 方法签名 | 功能说明 | 详细步骤和逻辑 | 参数、返回值对象 | 关键点 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [方法签名] | [功能] | [实现步骤，标注变更类型] | [参数、返回值对象] | [关键点] | [需求X.X/技术方案X.X] | 新增/修改/复用 |

**关键方法说明**：

[对于包含核心业务逻辑或复杂逻辑的方法，详细展开说明，不能只写参考XXX]

**[methodName(参数) → 返回值] - [功能]**:

**详细实现步骤**:
1. [步骤1，标注新增/复用/修改]
2. [步骤2]
3. ...

**关键点**:
- [关键点1，如使用的设计模式]
- [关键点2，如状态流转规则]
- [关键点3，如异常处理]

**关键代码**:

```java
// 列出5-10行关键代码或者SQl，体现方法核心逻辑、设计模式使用、关键调用
```

```xml
<!--列出5-10行关键代码或者SQl，体现方法核心逻辑、设计模式使用、关键调用-->
```

#### 2.3.2 [继续列出其他持久层服务]

---

### 2.4 领域模型层[依据应用架构调整名称]
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `领域模型层规范` 完整内容并严格遵守-->

[用表格列出所有领域模型（Domain对象、ValueObject、聚合、聚合根等），为新增和修改的类提供关键代码。如无领域模型，省略本章节]

| 类名 | 文件路径 | 所有字段和类型 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- |
| [领域模型类名] | [完整路径] | [字段名:字段类型，标注变更类型] | [技术方案X.X/规范文档] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如聚合根标识、值对象定义、领域行为等，不能只写参考XXX]

#### 2.4.1 [名称]实现方案（[新增/修改]）

**实现要点**:
- **相关注解**: [列出注解]
- **相关依赖**: [列出依赖]

**方法清单**:

| 方法签名 | 功能说明 | 详细步骤和逻辑 | 参数、返回值对象 | 关键点 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [方法签名] | [功能] | [实现步骤，标注变更类型] | [参数、返回值对象] | [关键点] | [需求X.X/技术方案X.X] | 新增/修改/复用 |

**关键方法说明**：

[对于包含核心业务逻辑或复杂逻辑的方法，详细展开说明，不能只写参考XXX]

**[methodName(参数) → 返回值] - [功能]**:

**详细实现步骤**:
1. [步骤1，标注新增/复用/修改]
2. [步骤2]
3. ...

**关键点**:
- [关键点1，如使用的设计模式]
- [关键点2，如状态流转规则]
- [关键点3，如异常处理]

**关键代码**:

```java
// 列出5-10行关键代码，体现方法核心逻辑、设计模式使用、关键调用
```

#### 2.4.2 [继续列出其他领域模型层服务]

---

### 2.5 数据仓储层[依据应用架构调整名称]
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `数据仓储层规范` 完整内容并严格遵守-->

[用表格列出所有仓储层接口和实现（Repository接口+实现类等），为新增和修改的类提供关键代码。如无仓储层，省略本章节]

| 名称 | 文件路径 | 所有方法签名 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- |
| [实现类名] | [完整路径] | [方法签名，标注变更类型] | [技术方案X.X/规范文档] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如Repository职责、依赖等，不能只写参考XXX]

#### 2.5.1 [名称]实现方案（[新增/修改]）

**实现要点**:
- **相关注解**: [列出注解]
- **相关依赖**: [列出依赖]

**方法清单**:

| 方法签名 | 功能说明 | 详细步骤和逻辑 | 参数、返回值对象 | 关键点 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [方法签名] | [功能] | [实现步骤，标注变更类型] | [参数、返回值对象] | [关键点] | [需求X.X/技术方案X.X] | 新增/修改/复用 |

**关键方法说明**：

[对于包含核心业务逻辑或复杂逻辑的方法，详细展开说明，不能只写参考XXX]

**[methodName(参数) → 返回值] - [功能]**:

**详细实现步骤**:
1. [步骤1，标注新增/复用/修改]
2. [步骤2]
3. ...

**关键点**:
- [关键点1，如使用的设计模式]
- [关键点2，如状态流转规则]
- [关键点3，如异常处理]

**关键代码**:

```java
// 列出5-10行关键代码，体现方法核心逻辑、设计模式使用、关键调用
```

#### 2.5.2 [继续列出其他数据仓储层服务]

---

### 2.6 领域服务层[依据应用架构调整名称]
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `领域服务层规范` 完整内容并严格遵守-->

[用表格列出所有领域服务接口、实现、事件等（DomainService），为新增和修改的类提供关键代码。如无领域服务层，省略本章节]

| 类名 | 文件路径 | 功能说明 | 依赖服务 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- |
| [DomainService接口/实现类名/事件] | [完整路径] | [功能说明] | [依赖服务] | [技术方案X.X] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如领域服务职责、注解、依赖注入等，不能只写参考XXX]

#### 2.6.1 [服务名称]实现方案（[新增/修改]）

**实现要点**:
- **相关注解**: [列出注解]
- **相关依赖**: [列出依赖的Repository、外部服务、工具类等]

**方法清单**:

| 方法签名 | 功能说明 | 详细步骤和逻辑 | 参数、返回值对象 | 关键点 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [方法签名] | [功能] | [实现步骤，标注变更类型] | [参数、返回值对象] | [关键点] | [需求X.X/技术方案X.X] | 新增/修改/复用 |

**关键方法说明**：

[对于包含核心业务逻辑或复杂逻辑的方法，详细展开说明，不能只写参考XXX]

**[methodName(参数) → 返回值] - [功能]**:

**详细实现步骤**:
1. [步骤1，标注新增/复用/修改]
2. [步骤2]
3. ...

**关键点**:
- [关键点1，如使用的设计模式]
- [关键点2，如状态流转规则]
- [关键点3，如异常处理]

**关键代码**:

```java
// 列出5-10行关键代码，体现方法核心逻辑、设计模式使用、关键调用
```

#### 2.6.2 [继续列出其他领域服务]

---

### 2.7 应用服务层[依据应用架构调整名称]
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `应用服务层规范` 完整内容并严格遵守-->

[用表格列出所有应用服务接口和实现（ApplicationService/Executor），为新增和修改的类提供关键代码。如无应用服务层，省略本章节]

| 类名 | 文件路径 | 功能说明 | 依赖服务 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- |
| [ApplicationService/Executor接口/实现类名] | [完整路径] | [功能说明] | [依赖服务] | [技术方案X.X] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如应用服务职责（流程编排、事务控制）、注解、依赖注入等，不能只写参考XXX]

#### 2.7.1 [服务名称]实现方案（[新增/修改]）

**实现要点**:
- **相关注解**: [列出注解]
- **相关依赖**: [列出依赖的DomainService、Repository、外部服务、工具类等]

**方法清单**:

| 方法签名 | 功能说明 | 详细步骤和逻辑 | 参数、返回值对象 | 关键点 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [方法签名] | [功能] | [实现步骤，标注变更类型] | [参数、返回值对象] | [关键点] | [需求X.X/技术方案X.X] | 新增/修改/复用 |

**关键方法说明**：

[对于包含复杂流程编排或多服务协调的方法，详细展开说明，不能只写参考XXX]

**[methodName(参数) → 返回值] - [功能]**:

**详细实现步骤**:
1. [步骤1，标注新增/复用/修改]
2. [步骤2]
3. ...

**关键点**:
- [关键点1，如事务边界]
- [关键点2，如流程编排]
- [关键点3，如异常处理和补偿]

**关键代码**:

```java
// 列出5-10行关键代码，体现流程编排、事务控制、服务调用
```

#### 2.7.2 [继续列出其他应用服务]

---

### 2.8 Web接口层
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `Web接口规范` 完整内容并严格遵守-->

[用表格列出所有Web接口类（Controller/RestController），为新增和修改的类提供关键代码。如无Web接口，省略本章节]

| 类名 | 文件路径 | 功能说明 | 请求路径前缀 | 依赖服务 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [Controller名称] | [完整路径] | [功能说明] | [请求路径] | [依赖的Service] | [技术方案X.X/Web接口设计] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如继承的基类、注解、统一返回格式、参数校验等，不能只写参考XXX]

#### 2.8.1 [Controller名称]实现方案（[新增/修改]）

**实现要点**:
- **相关注解**: [列出注解]
- **相关依赖**: [列出依赖的Service]

**方法清单**:

| 方法签名 | 功能说明 | 请求路径 | HTTP方法 | 参数、返回值对象 | 关键点 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| [方法签名] | [功能] | [完整请求路径] | [GET/POST/PUT/DELETE] | [参数、返回值对象] | [关键点] | [需求X.X/Web接口设计] | 新增/修改/复用 |

**关键代码**:

```java
// 列出5-10行关键代码，体现接口定义、注解、参数校验、统一返回
```

#### 2.8.2 [继续列出其他Controller]

---

### 2.9 RPC接口层
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `RPC接口规范` 完整内容并严格遵守-->

[用表格列出所有RPC接口类（Facade/Service），为新增和修改的类提供关键代码。如无RPC接口，省略本章节]

| 类名 | 文件路径 | 功能说明 | 依赖服务 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- |
| [RPC接口/实现类名] | [完整路径] | [功能说明] | [依赖的Service] | [技术方案X.X] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如RPC注解（Dubbo/HSF）、版本管理、超时配置等，不能只写参考XXX]

#### 2.9.1 [RPC接口名称]实现方案（[新增/修改]）

**实现要点**:
- **相关注解**: [列出RPC注解]
- **配置项**: [版本号、超时时间等]
- **相关依赖**: [列出依赖的Service]

**方法清单**:

| 方法签名 | 功能说明 | 服务标识 | 参数、返回值对象 | 关键点 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [方法签名] | [功能] | [Dubbo接口路径/HSF服务名] | [参数、返回值对象] | [关键点] | [技术方案X.X] | 新增/修改/复用 |

**关键代码**:

```java
// 列出5-10行关键代码，体现RPC接口定义、注解、方法签名
```

#### 2.9.2 [继续列出其他RPC接口]

---

### 2.10 传输对象与转换器
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取相关内容并严格遵守-->

[用表格列出所有传输对象（DTO/VO/Request/Response/Command/Query）与转换器（Assembler/Convert），为新增和修改的类提供关键代码。如无传输对象，省略本章节]

| 名称| 文件路径 | 类型 | 功能说明 | 所有字段和类型 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [传输对象名称] | [完整路径] | [DTO/VO/Request/Response] | [功能说明] | [字段名:字段类型，标注变更类型] | [技术方案X.X/Web接口设计] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如命名规范、校验注解（@Valid/@NotNull等）、脱敏注解等，不能只写参考XXX]

**关键代码**:

```java
// 列出5-10行关键代码，体现类声明、核心字段定义、校验注解
```

| 名称| 文件路径 | 类型 | 功能说明 | 所有方法 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [转换器/组装器名称] | [完整路径] | [Assembler/Convert] | [功能说明] | [方法与逻辑] | [技术方案X.X/Web接口设计] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如命名规范、校验注解（@Valid/@NotNull等）、脱敏注解等，不能只写参考XXX]

**关键代码**:

```java
// 列出5-10行关键代码，体现类声明、核心字段定义、校验注解
```

---


### 2.11 [继续列出识别到的其他必要的内容...]
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，
      1. 如有，读取 `XXX层规范` 完整内容并严格遵守
      2. 仿照模版中以上列出层的格式编写
-->

---

### 2.12 其他功能

#### 2.12.1 消息处理
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `技术栈要求` 和 `middleware-and-base（基础产品封装）` 相关内容并严格遵守-->

[用表格列出所有消息生产者和消费者（MQ Producer/Consumer、EventListener），为新增和修改的类提供关键代码。如无消息处理，省略本小节]

| 类名 | 文件路径 | Topic/事件类型 | 处理逻辑 | 参数对象 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [Producer/Consumer类名] | [完整路径] | [Topic名称/事件类型] | [处理逻辑简述] | [消息对象] | [技术方案X.X] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如MQ框架注解、消费模式、幂等处理、重试机制等，不能只写参考XXX]

##### 2.12.1.1 [名称]实现方案（[新增/修改]）

**实现要点**:
- **相关注解**: [列出注解]
- **相关依赖**: [列出依赖]

**方法清单**:

| 方法签名 | 功能说明 | 详细步骤和逻辑 | 参数、返回值对象 | 关键点 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [方法签名] | [功能] | [实现步骤，标注变更类型] | [参数、返回值对象] | [关键点] | [需求X.X/技术方案X.X] | 新增/修改/复用 |

**关键方法说明**：

**[methodName(参数) → 返回值] - [功能]**:

**详细实现步骤**:

**关键点**:

**关键代码**:

```java
// 列出5-10行关键代码，体现方法核心逻辑、设计模式使用、关键调用
```

##### 2.12.1.2 [继续列出其他消息处理服务]

---

#### 2.12.2 定时任务
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `技术栈要求` 和 `middleware-and-base（基础产品封装）` 相关内容并严格遵守-->

[用表格列出所有定时任务（ScheduledTask/Job），为新增和修改的类提供关键代码。如无定时任务，省略本小节]

| 类名 | 文件路径 | 执行周期 | 处理逻辑 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- |
| [定时任务类名] | [完整路径] | [Cron表达式] | [处理逻辑简述] | [技术方案X.X] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如任务调度框架注解、Cron配置、并发控制、异常处理等，不能只写参考XXX]

##### 2.12.2.1 [名称]实现方案（[新增/修改]）

**实现要点**:
- **相关注解**: [列出注解]
- **相关依赖**: [列出依赖]

**方法清单**:

| 方法签名 | 功能说明 | 详细步骤和逻辑 | 参数、返回值对象 | 关键点 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [方法签名] | [功能] | [实现步骤，标注变更类型] | [参数、返回值对象] | [关键点] | [需求X.X/技术方案X.X] | 新增/修改/复用 |

**关键方法说明**：

**[methodName(参数) → 返回值] - [功能]**:

**详细实现步骤**:

**关键点**:

**关键代码**:

```java
// 列出5-10行关键代码，体现方法核心逻辑、设计模式使用、关键调用
```

##### 2.12.2.2 [继续列出其他定时任务服务（缓存等）]

---

#### 2.12.3 外部服务集成
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `外部服务集成` 与 `业务能力服务` 相关内容并严格遵守-->

[用表格列出所有外部服务调用（Client/Adapter），为新增和修改的类提供关键代码。如无外部服务集成，省略本小节]

| 类名 | 文件路径 | 外部服务 | 集成方式 | 降级策略 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- | --- |
| [Client/Adapter类名] | [完整路径] | [外部服务名称] | [HTTP/RPC/SDK] | [降级方案] | [技术方案X.X] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如HTTP客户端、超时配置、重试机制、降级策略等，不能只写参考XXX]

**关键代码**:

```java
// 列出5-10行关键代码，体现外部服务调用、超时配置、降级逻辑
```

---

#### 2.12.4 [继续列出其他功能内容...]

---



### 2.13 异常处理
<!-- 编写要求：读取 `.specify/memory/AGENTS.md` 应用知识索引，如有，读取 `异常处理` 相关内容并严格遵守-->

[用表格列出所有自定义异常类和全局异常处理器，为新增和修改的类提供关键代码。如无异常处理，省略本章节]

| 类名 | 文件路径 | 类型 | 说明 | 来源 | 变更类型 |
| --- | --- | --- | --- | --- | --- |
| [异常类/处理器名称] | [完整路径] | [自定义异常/全局处理器] | [功能说明] | [技术方案X.X/规范文档] | 新增/修改/复用 |

**实现说明**: [列出实现关键点，如异常体系、错误码规范、全局处理器等，不能只写参考XXX]

**关键代码**:

```java
// 列出5-10行关键代码，体现异常定义、错误码、全局处理逻辑
```

---

### 2.14 [继续列出其他必要内容...]

---

## 3. 跨模块集成

### 3.1 模块依赖关系

[用表格列出模块间的依赖关系。如无跨模块依赖，省略本章节]

| 调用模块 | 被调用模块 | 调用场景 | 集成方式 | 数据流向 | 来源 |
| --- | --- | --- | --- | --- | --- |
| [模块名称] | [模块名称] | [调用场景] | [RPC/消息/HTTP] | [数据流向] | [技术方案X.X] |

**实现说明**: [列出跨模块集成的关键点]

---

### 3.2 跨模块集成方案

[对于复杂的跨模块集成，详细说明集成方案。如无复杂集成，省略本小节]

**[模块A]调用[模块B] / [模块A]通过[事件]通知[模块B]**:

**集成方式**: [RPC调用/消息通知/HTTP调用/领域事件/EventBus]

**详细说明**: [列出集成步骤、数据转换、异常处理等]

**关键代码**:

```java
// 列出5-10行关键代码，体现跨模块调用、数据转换、异常处理
```

---

## 4. 配置清单

[列出需要在项目配置文件中完成的配置，只列出有明确来源的配置。如无需配置或配置在外部系统，省略本章节]

```properties
# [配置项说明]
[配置项]=[配置值]

# [配置项说明]
[配置项]=[配置值]
```

---

## 5. 开发顺序

[按分层开发方式，分阶段列出开发顺序。每个阶段不超过2人日。只包含代码开发任务，不包含执行QL、测试、联调、上线等任务]

**阶段1: [开发内容概述，如：基础定义和数据模型开发]**
1. 新增/修改 [文件路径] - [说明]
2. 新增/修改 [文件路径] - [说明]
3. ...

**阶段2: [开发内容概述，如：持久层和服务层开发]**
1. 新增/修改 [文件路径] - [说明]
2. 新增/修改 [文件路径] - [说明]
3. ...

**阶段3: [开发内容概述]**
1. ...

---

## 附录


### 附录A-工具类清单

[列出本次开发需要使用的工具类。如无工具类使用，省略本小节]

| 工具类名称 | 功能说明 | 使用位置 | 来源 |
| --- | --- | --- | --- |
| [工具类名称] | [功能说明] | [使用位置] | [代码库路径/技术方案X.X] |

---

### 附录B-模版清单

[列出本次开发需要使用的模版，如果没有明确的出处，标记为未定义，不要自行发挥。如不含此类内容，省略本章节]

| 模版标识 | 模版名称 | 使用场景 | 使用位置 | 来源 |
| --- | --- | --- | --- | --- |
| [模版标识] | [模版名称] | [使用场景,简述] | [使用位置] | [引用来源] |

**[模版标识] - [模版名称]**:

[详细描述模版、参数]

---

### 附录C-技术方案与需求差异说明

[列出技术方案与结构化需求的差异，说明以技术方案为准的原因。如无差异，省略本小节]

| 差异项 | 结构化需求 | 技术方案 | 说明 |
| --- | --- | --- | --- |
| [差异项] | [需求描述] | [技术方案描述] | [差异原因] |